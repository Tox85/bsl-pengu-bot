# Exchange configuration
BYBIT_API_KEY=
BYBIT_API_SECRET=
HUB_WITHDRAW_AMOUNT=5                  # Amount in ETH to withdraw to the hub each cycle when Bybit keys are present

# Wallet storage & derivation
STRATEGY_MNEMONIC="word1 word2 ..."     # Mnemonic that deterministically derives the hub + 99 satellites
HUB_WALLET_INDEX=0                     # Index (0-99) promoted as the hub wallet
HUB_WALLET_PASSWORD=change-me-please   # >= 12 characters, used to encrypt the wallet store
HUB_WALLET_STORE=./data/wallets.enc    # Path to the encrypted wallet registry

# Optional base funding key for dry-runs without Bybit
BASE_FUNDING_PRIVATE_KEY=0x...

# Networks
RPC_BASE=https://mainnet.base.org
RPC_ABSTRACT=https://api.mainnet.abs.xyz
CHAIN_ID_BASE=8453
CHAIN_ID_ABSTRACT=2741
GAS_PRICE_GWEI=0.3

# Strategy & execution
BRIDGE_SLIPPAGE_BPS=80                 # 0.8%
SWAP_SLIPPAGE_BPS=100                  # 1.0%
UNISWAP_ROUTER_ADDRESS=0x...
UNISWAP_PAIR_ADDRESS=0x...
REBALANCE_PRICE_THRESHOLD_PERCENT=10   # Trigger rebalance when price moves more than 10%
FEE_GAS_MULTIPLE_TRIGGER=3             # Recycle fees when >3× gas cost
PENGU_TO_ETH_FEE_SWAP_PERCENT=30       # Portion of Pengu fees swapped back to ETH for safety
FEE_REINVEST_PERCENT=60                # Share of harvested fees compounded back into the LP
LIQUIDITY_UTILIZATION_PERCENT=80       # Share of balances that are deployed to LP positions
SATELLITE_VARIANCE_MIN=0.85            # Min randomisation factor when funding satellites
SATELLITE_VARIANCE_MAX=1.15            # Max randomisation factor when funding satellites
PENGU_TOKEN_ADDRESS=0x9eBe3A824Ca958e4b3Da772D2065518F009CBa62
WRAPPED_ETH_ADDRESS=0x...

# Optional logging override
LOG_LEVEL=info
